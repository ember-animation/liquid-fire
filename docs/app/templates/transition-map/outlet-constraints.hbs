{{!-- template-lint-disable no-unbalanced-curlies --}}

<h2>
  Outlet Constraints
</h2>

<p>
  The following constraint applies to oulet names.
  It will only work with
  <code>
    \{{liquid-outlet}}
  </code>
  because only
  <code>
    \{{liquid-outlet}}
  </code>
  has access to it's own name.
  (The other helpers like
  <code>
    \{{liquid-if}}
  </code>
  ) don't know anything about outlets.)
</p>

<dl class="dl-horizontal">
  <dt>
    outletName
  </dt>
  <dd>
    Matches the outlet name at the start of a transition.
  </dd>
</dl>

<p>
  The default outlet name, if one isn't specified resolves to 'main'.
</p>

<p>
  Examples:
</p>

<CodeSnippet @name="outlet-match-examples.js" />