{{#each versions as |version| ~}}
  {{#if version.shouldRender ~}}
    {{#liquid-child version=version visible=false didRender="childDidRender" ~}}
      {{yield version.value ~}}
    {{/liquid-child}}
  {{/if}}
{{/each}}