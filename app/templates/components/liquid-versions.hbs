{{#each versions key="guid" as |version| ~}}
  {{#if version.shouldRender ~}}
    {{#liquid-child version=version didRender="childDidRender" class=class ~}}
      {{yield version.value ~}}
    {{/liquid-child}}
  {{/if}}
{{/each}}
