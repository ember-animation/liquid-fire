{{#each versions as |version| ~}}
  {{#if version.shouldRender ~}}
    {{#liquid-child version=version visible=false didRender="childDidRender" class=innerClass ~}}
      {{yield version.value ~}}
    {{/liquid-child}}
  {{/if}}
{{/each}}
