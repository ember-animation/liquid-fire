// This is an empty stub, the client application should introduce
// their own version of this file and use it to configure their
// transitions.
import Transitions from "./libs/liquid-fire";
export default Transitions.map(function(){});
