{{#each items as |item| ~}}
  {{#-lf-list-element entering=(action "childEntering")
                       leaving=(action "childLeaving")  ~}}
    {{yield item ~}}
  {{/-lf-list-element ~}}
{{/each}}
